import React from 'react';
import PropTypes from 'prop-types';

const ProfileItem = () => async (dispatch) => {
  return (
    <div>
      <h1>ProfileItem</h1>
    </div>
  );
};

export default ProfileItem;
